# Syncord settings

Place deployment configuration and release notes here. Keep secrets such as GitHub tokens, SSH keys, and private URLs outside this repository.

The build script uses these defaults:

- Source: `../src/Vencord` and `../src/Installer`
- Published files: `../build`
- Git branch: `main`
